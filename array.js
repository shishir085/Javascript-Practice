

function sumArray(array) {
    let sum = 0;
    for (let i = 0; i < array.length; i++) {
      sum += array[i];
    }
    return sum;
  }
  
  const numbers = [1, 2, 4, 4, 5];
  const result = sumArray(numbers);
  console.log(result); // Output: 15

  let a = console.log('hellffo')

